# Agent skills

Task-specific runbooks for coding agents working in this fork. Each skill is a directory with `SKILL.md` (YAML frontmatter + instructions).

**Paths:** `.claude/skills` is a symlink to `.cursor/skills`. Edit and commit under `.cursor/skills/` only.

## Index

| Skill                                           | When to use                                                             |
| ----------------------------------------------- | ----------------------------------------------------------------------- |
| [rafaelgaspar-fork](rafaelgaspar-fork/SKILL.md) | Fork workflow — feat branches, `rafaelgaspar` integration, GHCR publish |
